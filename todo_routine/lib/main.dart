import 'package:flutter/material.dart';
import 'package:todo_routine/todo_routine_app.dart';

//import 'package:flutter/services.dart';

void main() {
  /*
  WidgetsFlutterBinding.ensureInitialized(); // make sure that flutter environment is fully initialized before configuration 

  SystemChrome.setPreferredOrientations([DeviceOrientation.portraitUp]) // set only horizontal orientation 
      .then((fn) { //The 'then' method run app after setting the orientation.
  });
  */
  runApp(TodoRoutineApp());
}
